{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mkd.local/knowledge-system/m1-c-review-record.schema.json",
  "type": "object",
  "required": [
    "schemaVersion",
    "reviewId",
    "roleId",
    "assignee",
    "releaseId",
    "siteDigest",
    "assignedAt",
    "independenceAttestation",
    "state",
    "decision",
    "signedAt",
    "automatedEvidenceRefs",
    "reviewItems",
    "findings"
  ],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "reviewId": { "type": "string", "pattern": "^M1-C-REVIEW-ROLE-[A-Z0-9-]+$" },
    "roleId": { "type": "string", "pattern": "^ROLE-[A-Z0-9-]+$" },
    "assignee": { "type": "string", "minLength": 2 },
    "releaseId": { "type": "string", "pattern": "^mkd-m1c-[a-f0-9]{16}$" },
    "siteDigest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "assignedAt": { "type": "string", "pattern": "^20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$" },
    "independenceAttestation": { "type": ["boolean", "null"] },
    "state": { "enum": ["assigned", "completed"] },
    "decision": { "enum": ["pending", "accepted", "rejected"] },
    "signedAt": {
      "anyOf": [
        { "type": "string", "pattern": "^20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$" },
        { "type": "null" }
      ]
    },
    "automatedEvidenceRefs": {
      "type": "array",
      "minItems": 2,
      "items": { "type": "string", "minLength": 3 },
      "uniqueItems": true
    },
    "reviewItems": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "required": ["check", "status", "note"],
        "properties": {
          "check": { "type": "string", "minLength": 3 },
          "status": { "enum": ["pending", "passed", "failed"] },
          "note": { "type": ["string", "null"] }
        },
        "additionalProperties": false
      }
    },
    "findings": {
      "type": "array",
      "items": { "type": "string", "minLength": 3 }
    }
  },
  "additionalProperties": false
}
