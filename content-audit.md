# MKD Guide 全站内容审计

审计日期：2026-08-02<br>
审计范围：`docs/knowledge/*.md` 共 26 个模块<br>
证据口径：本地源文件、Markdown AST、构建 HTML、确定性 fixture 与浏览器回执；不把计划、示意代码或构建成功当作生产完成证据。

## 执行摘要

### 事实

- 初始站点有 18 个页面被错误的 `````text`` / `````txt`` 结束围栏截断。确定性修复器实际修复 **203 处**；计划中的“265 处”为早期文本扫描估计，不作为最终回执。
- 旧审计器曾把 `Array.push()` 的数字返回值误当作 AST 遍历控制，因而虚报 **2,107 个代码块 / 182 个链接**。修复后当时的 G0 基线为 **26 页、257 个 H2、443 个 H3、352 个代码块与 161 个 Markdown 链接**，结果为 0 error / 0 warning；后续每次受审查的增量继续显式更新 `knowledge-system/audit-baseline.json`。
- 26 页均具备统一 frontmatter 契约和“来源与复核”区；新增 `docId`、`displayNumber`、`route`、`learningOrder`，首页目录直接读取该契约。
- 文档身份注册表覆盖 26/26 页面，显示编号为 00–23 加附录 A/B，学习顺序为 0–25；原公开路由全部保留。“第八章（附录）”已归类为附录 B，并保留旧 H1 锚点。
- 概念注册表已升级为 schema v2：13 个核心概念、22 个别名、13 个首次定义、13 个跨章语义使用样本、26 条前置边、8 对不可混同关系与 31 对相关导航；四处章节标签错配和 7 个“定义前提及但无回链”缺口均已进入自动门禁。
- G2.4 后当前基线为 26 页、265 个 H2、443 个 H3、352 个代码块与 167 个 Markdown 链接；新增的 4 个验收标题均来自三个试点页面和附录 A 的可见工作台。
- 验收注册表现覆盖安全、成本、评估三章：12 个固定本地用例含 8 个负例，3/3 可复放、0/3 已验收；数据授权、阈值批准、责任接受和最终回执四类阻断均由机器计算。
- 页面级 smoke 状态仍只有成本模型与最小 mock Pipeline 两页。G2.2 另为安全治理和质量评估增加了断言级 L2 fixture，但两页继续保持 `verification: pending`，没有用局部测试批量抬高整页状态。
- VOC 章节已改为合成案例，失败案例已改为复合教学案例；原先缺乏原始证据的精确成果叙述不再作为真实结果陈述。
- OpenAI 服务示例已迁移到 Responses API；Qdrant 检索示例已迁移到 `query_points(...).points`。本地 OpenAI-compatible 端点保留 Chat Completions 兼容调用，并明确限定在 localhost。
- 运维删除示例已补快照、dry-run、显式目标、双重确认、回滚和观察窗，且不再用“覆盖重建”实现迁移。
- VitePress 1.6.4 内建 preview 会忽略传入 host 并监听所有接口；本地静态预览已替换为显式回环服务器，`docs:preview-isolation` 验证局域网地址不可达并进入总门禁。

### 推断

- P0“页面看似构建成功但正文未进入 HTML/搜索”的假绿已经从源结构上解除；最终发布仍必须以 `docs:headings` 的 26/26 构建后对账为准。
- 目前站点已经从“长文集合”进入“证据透明的方案库”，但还不是 26 个全部可运行或可验收的工程产品。
- G0/G1 已解决“计量是否可信”和“文档身份是否稳定”，G2.1–G2.4 已建立首批 claim、责任槽位、L2 fixture、概念关系与验收状态机；下一阶段的主要结构性缺口是学习/检索验证、其余 23 章的断言迁移、71 个未审查概念线索和组织审批证据。
- 主要剩余风险已从 Markdown 解析转移到外部事实漂移、第三方集成未执行、危险操作缺少真实演练回执，以及样例数据/评估集缺少授权来源。

### 不确定项

- 供应商价格、模型版本、开源项目活跃度和 benchmark 会持续漂移；未在本地 fixture 中固化的值均不得视为当前事实。
- `pending` 章节中的代码多数只完成接口形态复核，没有真实凭据、GPU、数据库或供应商环境回执。
- 外链门禁能区分成功、真实 404/410、访问受限和暂时网络不可达，但访问受限不等于页面内容已复核。
- `documents.yml` 中的先修关系是本轮建立的可检查阅读假设，尚未通过学习者行为或任务完成率验证。
- 本轮没有部署、推送、合并、生产切流、真实数据导入或远程服务调用。

## 证据成熟度总账

| 状态 | 数量 | 解释 |
|---|---:|---|
| 原理 | 2 | 定义问题与边界，没有工程闭环 |
| 方案 | 22 | 有结构与参考实现，但没有足够运行回执 |
| 可运行 | 2 | 仅确定性成本 fixture 与 mock Pipeline 最小闭环 |
| 可验收 | 0 | 三个试点已有固定集、负例和本地回归，但均缺业务授权、批准阈值、责任接受与最终回执 |
| 待复核 | 24 | 首页明确显示“待复核” |
| 烟测通过 | 2 | 仅代表 frontmatter 中列出的本地证据范围 |

成熟度表示本地内容状态，不表示部署、上线或生产完成。

## 26 模块整改与落地距离

| 模块 | 当前 → 目标 | 已落实事实 | 脆弱点 / 缺失项 | 下一验收动作 |
|---|---|---|---|---|
| 00 导论 | 原理 → 方案 | 已纳入契约、边界声明与复核区 | 2026 全景仍缺逐项一手来源和更新时间策略 | 给每个易漂移判断绑定来源、截止日期和“何时失效”条件 |
| 01 认知框架 | 方案 → 方案 | 围栏恢复；DIKW 修正为 Data/Information/Knowledge/Wisdom 四层；DIKW、LoD 与 Skill Distillation 已有首次定义 | 概念图仍是语义样本；71 个跨页词面线索尚未逐项判定 | 优先审查 RAG、Agent、GraphRAG、MCP 的高扇出用法并登记符合/改写/删除结论 |
| 02 决策矩阵 | 方案 → 方案 | 围栏恢复，矩阵正文可见 | 输入、输出、否决条件缺少可复放 fixture | 固化 3 个完整决策输入与预期输出并做快照测试 |
| 03 场景 SOP | 方案 → 可运行 | 围栏恢复，旧绝对可运行声明已降级 | 10 场景未逐一覆盖 GPU / 无 GPU 与错误路径 | 每个场景增加最小输入、预期输出、资源前提和失败 fixture |
| 04 全链路架构 | 方案 → 可运行 | 明确 Stage 0 门禁 + Stage 1–6 执行，消除“五/六阶段”歧义 | 阶段接口和数据契约未机器校验 | 为每阶段定义 schema、错误码、前后置条件与契约测试 |
| 06 GraphRAG | 方案 → 可运行 | 围栏恢复，重复编号改为 6.9/6.10 并保留旧锚点 | 没有最小图谱、索引与质量评估回执 | 增加小型 fixture，验证建索引、查询、引用和质量阈值 |
| 05 安全合规 | 方案 → 可运行 | 增加司法辖区边界与一手来源；5 例验收集合含 3 个负例，本地复放 5/5、基线差异 0 | 仍是合成 L2；数据未获业务授权、阈值未批准、0/4 责任接受且无最终回执 | 由具名角色接受责任，选择目标辖区并用已授权样本在隔离环境完成演练回执 |
| 07 Agent + MCP | 方案 → 可运行 | 围栏与标题层级恢复 | schema、鉴权、超时、错误、幂等只停留在示意 | 用 mock MCP server 覆盖成功、超时、重试、重复调用和拒绝路径 |
| 08 LoD 理论 | 方案 → 方案 | 7/8 章节顺序漂移已修 | 选型阈值与推导输入不可复算 | 增加一套端到端推导案例和阈值敏感性分析 |
| 附录 B 工具手册 | 方案 → 方案 | 围栏恢复，正式归类为附录 B，原路由与旧 H1 锚点保留 | 版本、退出成本与“不选条件”易漂移 | 逐工具记录官方版本日期、迁移出口和弃用信号 |
| 09 推理范式 | 方案 → 方案 | 补 9.7 证据边界、延迟预算与失败降级 | 延迟预算没有基准数据 | 用固定模型/硬件/输入集记录首 token、总时延和降级结果 |
| 11 Skill 蒸馏 | 方案 → 方案 | H2 统一为 11.1–11.11，去除 8.x 漂移 | 九仓库事实与方法示例边界仍需逐项来源 | 给仓库事实加 commit/date，方法示例单独标注 synthetic |
| 10 成本模型 | 可运行 → 可运行 | 参数化 fixture 与 4 例验收集合覆盖固定计算及 3 个输入负例；本地复放 4/4 | 真实价格、区域、税费、折扣未复核；阈值和预算责任未批准，最终回执缺失 | 录入目标区域当日官方价格和真实负载，由 FinOps 批准误差阈值后签发内容验收回执 |
| 12 E2E Pipeline | 可运行 → 可运行 | 标题收敛为最小样例；mock fixture 覆盖归一、检索、证据与拒答 | 正文 Python、供应商和数据库集成仍是示意 | 建最小工程目录，在无凭据 mock 环境跑一次完整命令并留回执 |
| 13 知识库演进 | 方案 → 可运行 | 围栏恢复，复核区明确禁止把计划视为执行 | 审批、冲突、回滚和发布门禁未形成状态机 | 用临时仓库测试提案、审批、拒绝、冲突和回滚；禁止自动提交默认开启 |
| 14 质量评估 | 方案 → 可验收 | 3 例版本化 mock 集覆盖回答、拒答、解析失败；2 个负例、本地复放 3/3、回归差异 0 | 缺获授权业务评估集、批准阈值、真实模型锁定、0/4 责任接受和最终回执 | 复用当前回归合同接入版本化业务数据集，并由具名角色批准阈值与错误成本 |
| 15 Prompts | 方案 → 可运行 | 围栏恢复，复核区声明代码边界 | 模型版本、注入防护、I/O schema 与黄金样例不完整 | 为核心 prompt 增加输入 schema、拒绝集、黄金输出和模型矩阵 |
| 16 VOC 案例 | 方案 → 可运行 | 改为母婴电器合成案例，删除无证据精确业务成果 | 没有授权原始数据，抽样偏差无法估计 | 用获授权离线样本复现聚合，并记录抽样、去重和偏差 |
| 17 失败案例 | 原理 → 方案 | 明确为复合教学案例，处置改为建议而非真实结果 | 时间线与因果关系不是可审计事件证据 | 如需称真实案例，必须附脱敏事件记录、因果证据与复发门禁 |
| 18 技术选型 I | 方案 → 可运行 | Qdrant 调用已迁移到当前 `query_points` 形态 | quickstart、star 数与 benchmark 未在统一环境复测 | 锁版本跑官方 quickstart；移除无日期 star/benchmark 或附快照 |
| 19 技术选型 II | 方案 → 可运行 | 占位假链接已清理；localhost 兼容调用边界明确 | 与第 18 章分工、统一基准和退出测试仍不足 | 建同一 fixture 横评两章候选并执行导出/迁移测试 |
| 20 运维 Runbook | 方案 → 可验收 | 删除流程补快照、dry-run、显式目标、确认、回滚、24h 观察窗 | 安全流程仅语法检查，未对临时环境演练 | 在一次性本地容器执行备份—删除—恢复演练并保存完整回执 |
| 21 数据采集 | 方案 → 可运行 | 假 GitHub 链接与 22–26 编号漂移已修，保留旧锚点 | ToS、robots、PII、限速和离线复现不完整 | 建合法离线 fixture；对每源记录条款、授权、限速与删除路径 |
| 22 Agent 设计 | 方案 → 可运行 | 尾章“27”改为 22.7；Qdrant 形态更新 | 状态机、多 Agent 恢复和审批轨迹没有烟测 | 用确定性状态图覆盖中断、重放、人工拒绝和幂等恢复 |
| 23 FT vs RAG | 方案 → 可运行 | OpenAI/Qdrant 接口形态更新，决策边界保留 | 论文数字、RAFT 结果和成本假设未本地复现 | 建决策 fixture，锁论文数据版本并做 RAFT 小样本烟测 |
| 附录验证框架 | 方案 → 可验收 | 新增三契约机器工作台与 `docs:acceptance`；固定集、负例、阈值、回归、责任和最终回执分开显示 | 只覆盖 3/26 页面，且 0/3 accepted；其余页面没有验收引用 | 先用 G2.5 黄金查询验证搜索/学习路径，再按风险扩展 acceptanceRef |

## 横切脆弱点

| 风险 | 当前控制 | 仍需补齐 |
|---|---|---|
| 内容假绿 | AST 围栏检查 + 逐页计量基线 + 源/HTML 标题对账 | 在 CI 中保存基线漂移解释与 26/26 回执 |
| 文档身份漂移 | `documents.yml` + `docs:identity` 分离稳定 ID、显示编号、路由与学习顺序 | 后续删除 legacy `chapter/order` 前先完成所有消费端迁移 |
| 语义错链 | `docs:semantic-links` 检查章节标签；`docs:concepts` 检查别名所有权、首次定义、跨章使用、前置环与对称关系 | 将 71 个未登记词面线索逐项迁移为语义使用、示例或删除结论 |
| 证据状态膨胀 | 非 pending 必须有 evidence；首页从 frontmatter 生成；三章已建立 claim/source/evidence/ownership 结构 | 将断言级契约迁移到其余 23 章，并逐步替换整页字符串 evidence |
| 验收状态越级 | `docs:acceptance` 重新计算固定集、负例、阈值、回归、责任与最终回执；页面提前升级会失败 | 三个试点仍需真实组织证据，其余 23 章尚未建立契约 |
| 外部事实漂移 | 易漂移章节加入截至日期与来源区 | 建定期复核 owner、失效日期和价格/版本快照 |
| 示例被误当生产代码 | 默认 `illustrative`，可运行声明需 smoke evidence | 逐片段标注验证级别，并把核心片段抽成 fixture |
| 危险操作 | 静态检查删除上下文；Runbook 补保护步骤 | 临时环境恢复演练与人工授权回执 |
| 搜索与可见性 | frontmatter 标题注入本地索引；P0 内容恢复 | 对关键查询增加结果快照与零结果告警 |
| Mermaid 可用性 | 近视口动态加载、严格模式、可访问弹层与源码回退 | 大型图在真实内容下验证缩放及暖白主题下的打印/导出 |
| 本地预览暴露 | 自有静态预览显式绑定 127.0.0.1；门禁验证非回环地址拒绝 | 保持 VitePress 1.6.4 期间持续运行隔离门禁；升级后重新核对上游行为 |

## 未解决 P3 清单

1. 24 个 pending 模块的外部事实尚未全部逐条绑定一手来源、owner 和失效日期；G2.2 只为三章建立 12 个角色映射，12 个角色仍待具名认领与接受。
2. 352 个代码块中大量仍是示意代码；当前片段门禁覆盖七个独立 fixture，并对 10 个代表性 Markdown 片段执行显式 `verify=syntax`。这 10 个样本通过不代表其余 342 个代码块已验证。
3. 供应商、GPU、向量数据库、浏览器采集和真实数据授权均未接入；这属于后续有权限环境的验收，不在本地实现范围内。
4. Lighthouse 分数、LCP/INP/CLS 为本机预览基线，不能替代部署后真实用户监控。
5. `npm audit` 报告 3 个无可用自动修复的间接依赖问题（2 moderate、1 high），链路为 VitePress 1.6.4 → Vite / esbuild；静态预览已通过真实回环隔离门禁缓解暴露面，仍需在保持版本约束下评估上游修复，不能用破坏性 major upgrade 冒充修复。Lighthouse 脚本直接使用的 `chrome-launcher@1.2.1` 已改为直接开发依赖，且 Node 运行时下限已固定为 `>=22.19.0`；这两项只解决安装可复现性，不关闭上游漏洞。
6. 13 个概念已建立首批“定义于、使用于、前置于、不可混同”语义图，但 97 个概念/文档词面线索中仍有 71 个未迁移；26 条前置边也没有学习效果验证。
7. 验收契约只覆盖 3/26 页面；三套固定集合仍是 synthetic-only，9 条阈值均为 illustrative，12 个角色 0 accepted，最终回执 0/3。回执结构已增加合同、用途、主体、批准角色、决策和 SHA-256 绑定；路径必须经 realpath 后仍位于仓库内，artifact 必须是字段精确匹配的 JSON v1，且只有 `L4-authorized-live` 可进入审批计算。跨合同/用途、错误 artifact、路径逃逸、低证据等级均有负例，四类正确绑定也有纯内存正向接口用例；这些只加固失败关闭，不能认证自然人或生成真实批准。

### 2026-08-03 P3 本地候选处置

- **本地已修复**：Node/Lighthouse 直接依赖契约、10 个代表性语法片段、验收回执的安全路径/精确 artifact/L4 身份绑定、首页 26 个目录链接的原生语义与移动端字段单次播报、搜索方向键选择的读屏播报，以及未交互前不加载搜索模块。
- **本地已验证但不抬级**：15 个版本化黄金查询、响应式四模式、搜索空状态/键盘/焦点恢复、暖白界面、存储与外部请求边界均有本地浏览器合同；仍只属于本地 L2 候选证据。
- **继续阻断**：第 1、3、4、6、7 项中的一手来源/责任接受、授权业务数据与真实集成、生产 RUM、学习效果、最终验收均需外部证据；第 5 项上游依赖风险仍开放。
- **交付门禁缺口**：当前 GitHub Pages 工作流只安装并构建，没有执行完整内容、浏览器、Lighthouse 与依赖审计。补齐 CI 需要独立 Git/交付授权，不在本次本地候选门禁内。

## 发布判定

- **本地内容可见性**：以最终 `docs:headings` 回执为准。
- **本地设计与交互**：以根目录 `design-qa.md` 的 `final result: passed` 为准。
- **生产状态**：未部署、未验证、未声明完成。

## G2.1 三章增量审计

| 模块 | 新增事实 | 仍未解决 |
|---|---|---|
| 05 安全合规 | 4 条稳定 claim；NIST/EU 两条为 L1；安全演练与示例期限两条保持 L0 | 未分配 owner；未签署辖区/角色判断；没有威胁模型、演练、销毁或控制验证回执 |
| 10 成本模型 | 4 条稳定 claim；USD 135、输入口径、缓存失败关闭三条为 L2；官方计费维度为 L1 | 真实价格、税费、区域、折扣、缓存资格、账单差异与预算审批未验证 |
| 14 质量评估 | 4 条稳定 claim；验收契约、当前缺口和阈值示意边界为 L1；代码状态为 L0 | 没有固定评估集、负例、批准阈值、mock provider 与机器可比较回归回执 |

G2.1 只改变证据粒度，没有把任何页面升级为 acceptance。完整断言与证据边界见 `g2-1-claim-pilot-audit.md`。

## G2.2 责任链与 L2 证据增量审计

### 事实

- `claims.yml` 升级为 schema v2.0；三章各定义内容维护、证据复核、测试维护和最终批准四个角色，共 12 个角色。12/12 claim 的四角色映射完整且互相分离。
- 仓库没有可验证的具名责任人接受记录，因此 12 个角色均保持 `role-mapped`，0 个 `accepted`。页面显示“4/4 角色已映射 · 0/4 已接受”，门禁持续 warning。
- `CLM-SEC-003`、`CLM-SEC-004` 与 `CLM-EVAL-004` 从 L0 提升到 L2，依据分别是安全治理 fixture/test 与评估回归 fixture/test；全局分布变为 L0=0、L1=6、L2=6、L3/L4=0。
- 安全 fixture 只处理纯本地对象，拒绝 `production` 数据标签，并要求隔离环境、受支持的数据分类与销毁回执模式；示例期限缺少完整生产字段时不能升级。
- 评估 fixture 使用三用例确定性 mock provider，覆盖有证据回答、证据不足拒答、JSON 解析失败与候选退化阻断，外部调用为 0。

### 推断

- 角色映射已经解决“谁负责哪一类动作”的结构问题，但没有解决“哪位自然人接受责任”的组织问题；后者仍是人工阻断项。
- 三条断言达到 L2 只证明仓库内 fail-closed 与回归合同可复现，不证明真实安全演练、RAGAS 集成、生产数据、法律合规或整章验收。

### 不确定项与下一证据

- 需要用户或组织提供具名 assignee，并以独立接受回执将角色从 `role-mapped` 推进到 `accepted`；不得由仓库自动生成姓名或接受日期。
- 安全章下一证据是辖区绑定、数据授权、威胁模型、隔离演练和销毁回执；评估章下一证据是获授权业务数据集、批准阈值、锁定模型和真实回归回执。
- 页面级成熟度没有变化：2 个 runnable、22 个 solution、2 个 principle、0 个 acceptance；24 页仍为 pending。

完整门禁判定见 `g2-2-ownership-evidence-audit.md`。

## G2.3 概念使用图增量审计

### 事实

- `concepts.yml` 升级为 schema v2；13/13 概念各有正文首次定义和一个跨章语义使用样本，定位由文档 ID、稳定锚点、邻近文本与学习顺序共同校验。
- 前置关系形成 26 条无环有向边；不可混同关系为 8 对双向边；相关导航为 31 对双向边。环、单向冲突、单向相关或不存在目标都会阻断。
- 词面扫描观察到 97 个概念/文档线索。7 个位于定义文档之前的线索均已直达首次定义，未回链数量为 0；其余 71 个未登记线索继续作为明确的迁移缺口。
- DIKW 从错误的“三层递进”统一为 Data、Information、Knowledge、Wisdom 四层，并与 LoD 的工程加工深度明确分离。
- 附录 A 的 `ConceptMap` 由注册表生成，不复制状态；桌面双列、390px 单列，13 张卡可展开并跳转定义或跨章用法。

### 推断与边界

- 当前可以证明核心概念的首批导航关系机器可检查，也能阻止无定义先用、成环和单向冲突；不能证明 71 个未迁移出现都符合定义。
- 概念图是结构治理证据，不是外部事实证据、生产结果或学习效果证据，因此没有提升任何页面成熟度。
- G2.4 acceptance contract 已完成三章试点；下一批应进入 G2.5 黄金查询与学习路径验证，并继续迁移 RAG、Agent、GraphRAG、MCP 高扇出用法。G2.3 完整矩阵见 `g2-3-concept-usage-graph-audit.md`。

## G2.4 机器可验收契约增量审计

### 事实

- `acceptance.yml` 登记安全、成本、评估三个 `repository-content` 契约；每个契约绑定固定数据集 SHA-256、基线回执 SHA-256、三类阈值、四角色责任链和最终回执状态。
- 三套集合合计 12 个本地合成用例、8 个负例；最新复放全部通过，基线通过率与候选通过率均为 100%，差异均为 0.0 pp，外部调用和外部副作用均为 0。
- 三个契约均计算为 `approval-blocked`：数据集不是获授权业务样本，9 条阈值未批准，12 个角色 0 accepted，3 个最终回执全部缺失。
- 页面若自报 `acceptance / acceptance-tested`，但契约计算态不是 `accepted`，`docs:acceptance` 会失败；注册表自报状态与 blockers 不一致同样失败。
- 附录 A 和三个试点页面的工作台均读取同一注册表与新鲜复放结果；没有用绿色“本地通过”替代警示态的组织阻断。

### 推断与边界

- 当前可证明“验收合同在本地可重放且会阻断退化/越级”，不能证明业务数据已授权、阈值适合真实错误成本、自然人已接受责任或章节已最终验收。
- G2.4 没有改变页面成熟度和 claim 证据等级；2 runnable、22 solution、2 principle、0 acceptance、24 pending 的总账继续成立。
- 下一批进入 G2.5：以 10–15 个版本化黄金查询检查搜索排名、推荐路径、零结果和错误分类，再用任务成功证据决定是否调整 `learningOrder`。完整判定见 `g2-4-acceptance-contract-audit.md`。
